# Suggested Commands

## Development
- `pnpm dev` - Start development server
- `pnpm build` - Build for production
- `pnpm start` - Start production server

## Code Quality
- Check package.json for lint/format commands

## Database
- `pnpm exec node -e "..." seed-data.js` - Seed database

## Git
- Standard git commands on Linux
